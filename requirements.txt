# ============================================================
# CMS Claims Analytics Pipeline -- Python dependencies
# Install into a virtual env:  pip install -r requirements.txt
# ============================================================

# --- AWS access (used by every script that talks to AWS) ---
boto3>=1.34
awswrangler>=3.7          # simple Athena -> pandas queries

# --- Data / ML (local prep + SageMaker launch) ---
pandas>=2.0
pyarrow>=15.0             # read/write Parquet
scikit-learn>=1.4         # train/test split, metrics, encoders
xgboost>=2.0              # model (also used locally to inspect artifact)
matplotlib>=3.8           # feature-importance plot
sagemaker>=2.200          # launch managed training jobs

# --- Dashboard ---
streamlit>=1.33
