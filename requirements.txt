# ============================================================
# CMS Claims Analytics Pipeline -- Python dependencies
# Install into a virtual env:  pip install -r requirements.txt
# ============================================================

# --- AWS access (used by every script that talks to AWS) ---
boto3>=1.34
awswrangler>=3.7          # simple Athena -> pandas queries

# --- Data / ML (local prep + local scoring) ---
# NOTE: no sagemaker SDK -- we call SageMaker via boto3 (stable across the
# SDK's v2/v3 churn). boto3 above is all that's needed to launch training.
pandas>=2.0
pyarrow>=15.0             # read/write Parquet
scikit-learn>=1.4         # train/test split, metrics
xgboost>=2.0              # load + score the trained model locally
matplotlib>=3.8           # feature-importance plot

# --- Dashboard ---
streamlit>=1.33
